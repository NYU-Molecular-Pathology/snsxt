# this task list will set up a new analysis directory
# and then run demo tasks that don't perform any analysis
# just to check if the program is working

# sns analysis setup tasks
sns:
  # StartSns:
  # DemoSnsTask:
  SnsValidate:

# downstream analysis tasks
tasks:

# compile the report for the analysis
setup_report: False
